# مشارکت در ZenOS

از علاقه شما به مشارکت در ZenOS متشکریم! این سند توضیح می‌دهد چگونه شروع کنید.

---

## چگونه مشارکت کنیم

### ۱. گزارش باگ

یک Issue باز کنید با:
- **توضیح:** چه اتفاقی افتاد در مقابل چه چیزی انتظار داشتید
- **مراحل بازتولید:** حداقل کد یا مراحل
- **محیط:** خانواده MCU، نسخه کامپایلر، سطح بهینه‌سازی
- **نسخه ZenOS:** خروجی `os_get_version_string()`

### ۲. پیشنهاد ویژگی

یک Issue باز کنید با:
- **مورد استفاده:** چرا این ویژگی مورد نیاز است
- **API پیشنهادی:** چگونه استفاده می‌شود
- **تأثیر ایمنی:** آیا بر مکانیسم‌های ایمنی تأثیر می‌گذارد؟

### ۳. ارسال کد

1. ریپو را Fork کنید
2. شاخه ویژگی بسازید: `git checkout -b feature/my-feature`
3. تغییرات خود را اعمال کنید
4. در صورت نیاز تست در `Src/main.cpp` اضافه کنید
5. مطمئن شوید تمام تست‌ها روی سخت‌افزار پاس می‌دهند
6. با پیام توصیفی Commit کنید
7. Push کنید و Pull Request باز کنید

---

## استانداردهای کد

### زبان

- **C++11** (بدون C++17، بدون RTTI، بدون استثنا)
- `static_assert` برای invariantهای زمان کامپایل
- RAII برای مدیریت منابع (قفل‌ها، بخش‌های حیاتی)
- `extern "C"` برای تمام توابع API عمومی

### نام‌گذاری

| عنصر | قرارداد | مثال |
|------|---------|------|
| توابع (C) | `snake_case` با پیشوند `os_` | `os_delay_ms()` |
| کلاس‌ها | `UPPER_CASE` | `OS_MUTEX`، `OS_EVENT` |
| ماکروها | `UPPER_CASE` با پیشوند `OS_` | `OS_SAFE`، `OS_LOCK` |
| پارامترهای قالب | `PascalCase` | `StackBytes`، `Capacity` |
| متغیرهای محلی | `snake_case` | `task_count`، `error_code` |

### قوانین ایمنی

1. **بدون تخصیص هیپ** — تمام حافظه باید به‌صورت ایستا تخصیص یابد
2. **بدون ایجاد پویای تسک** بعد از `os_start()`
3. **نسخه‌های امن ISR** باید در زمینه وقفه استفاده شوند (`signal_from_isr`، `put_from_isr`)
4. **بخش‌های حیاتی** باید کوتاه باشند (< 1ms توصیه شده)
5. **اندازه پشته‌ها** باید از طریق `os_get_stack_usage()` تأیید شوند
6. **تمام API عمومی** باید دارای نظرات سبک Doxygen باشد

### تست

- هر ویژگی جدید باید تست متناظر در `main.cpp` داشته باشد
- تست‌ها از طریق UART گزارش می‌دهند: `[PASS] #N: test_name` یا `[FAIL] #N: test_name`
- از ماکرو `TEST_ASSERT(condition)` برای assertions استفاده کنید
- از `OS_ERROR_EXPECTED { ... }` برای تست‌های تزریق خطا استفاده کنید
- مجموعه تست باید با 0 خطای غیرمنتظره پاس شود

### پیام‌های Commit

```
<type>: <توصیف کوتاه>

<بدنه اختیاری — چرا را توضیح دهید، نه چه را>

<پا اختیاری>
```

انواع: `feat`، `fix`، `docs`، `test`، `refactor`، `style`، `chore`

---

## ساختار پروژه

```
ZenOS/
├── ZenOS/
│   ├── ZenOS.hpp              # API عمومی (با احتیاط ویرایش شود — آفستهای اسمبلی)
│   ├── ZenOS_Config.hpp       # پیکربندی کاربر
│   ├── ZenOS_Port.hpp         # تشخیص سخت‌افزار
│   ├── ZenOS_Internal.hpp     # ساختارهای داخلی
│   ├── ZenOS_c.h              # هدر سازگار با C
│   ├── ZenOS.cpp              # هسته سیستم‌عامل
│   ├── ZenOS_Scheduler.cpp    # زمان‌بند
│   ├── ZenOS_Safety.cpp       # ویژگی‌های ایمنی
│   ├── ZenOS_IPC.cpp          # اصول اولیه IPC
│   └── ZenOS_Monitor.cpp      # پایش
├── README.md                  # README انگلیسی
├── README_FA.md               # README فارسی
├── API_TUTORIAL.md            # راهنمای API انگلیسی
├── API_TUTORIAL_FA.md         # راهنمای API فارسی
├── SAFETY_MANUAL.md           # راهنمای ایمنی انگلیسی
├── SAFETY_MANUAL_FA.md        # راهنمای ایمنی فارسی
├── CONTRIBUTING.md            # راهنمای مشارکت انگلیسی
├── CONTRIBUTING_FA.md         # راهنمای مشارکت فارسی
├── ZENOS_ADVANTAGES.md        # مقایسه فنی با RTOSهای دیگر
└── LICENSE                    # مجوز MIT
```

### مهم: چیدمان TCB

ساختار `TCB` در `ZenOS.hpp` دارای بررسی‌های `static_assert` است که آفستهای بایت را با کد اسمبلی تطبیق می‌دهد. **فیلدهای TCB را مرتب نکنید** — این باعث خطای کامپایل می‌شود. اگر نیاز به اضافه کردن فیلد دارید، آن را بعد از بخش حیاتی اسمبلی اضافه کنید.

---

## دریافت کمک

- [راهنمای API](API_TUTORIAL_FA.md) را بخوانید
- [راهنمای ایمنی](SAFETY_MANUAL_FA.md) را بخوانید
- برای سؤالات Issue باز کنید

---

## مجوز

با مشارکت، شما موافقت می‌کنید که مشارکت‌هایتان تحت مجوز MIT شود.
