# راهنمای ایمنی سیستم‌عامل ZenOS

> **سادگی — امنیت — سرعت**

**نسخه:** 1.0.0 | **تاریخ:** سپتامبر 2026 | **نویسنده:** گروه علمی و تحقیقاتی رایمون (Raymon Research Group) | **مجوز:** MIT

---

## ۱. هدف سند

این سند معماری ایمنی، مفروضات استفاده، محدودیت‌ها و محدودیت‌های عملیاتی سیستم‌عامل ZenOS را توصیف می‌کند. برای توسعه‌دهندگانی که ZenOS را در سیستم‌های تعبیه‌شده با اهمیت صحت، قطعیت و تحمل خطا ادغام می‌کنند، در نظر گرفته شده است.

ZenOS به هیچ استاندارد IEC 61508، ISO 26262 یا DO-178C **گواهی نشده** است. این سیستم‌عامل مکانیسم‌های ایمنی *بهترین تلاش* را ارائه می‌دهد که از ساخت سیستم‌های مرتبط با ایمنی **پشتیبانی** می‌کند، اما مسئولیت تأیید و گواهینامه بر عهده یکپارچه‌ساز است.

---

## ۲. مرور سیستم

ZenOS یک سیستم‌عامل بلادرنگ پیشگیرانه و اولویت‌محور برای میکروکنترلرهای ARM Cortex-M (M3, M4, M7) است که به C++11 نوشته شده و با API سازگار با C است.

### ۲.۱ سخت‌افزار پشتیبانی‌شده

| خانواده | هسته‌ها | فلش (نمونه) | RAM (نمونه) | حداکثر نواحی MPU |
|---------|--------|-------------|-------------|-------------------|
| STM32F1xx | 1 | 64 KB | 20 KB | 8 (Cortex-M3) |
| STM32F2xx | 1 | 1 MB | 128 KB | 8 |
| STM32F4xx | 1 | 1 MB | 128 KB | 16 |
| STM32F7xx | 1 | 2 MB | 256 KB | 16 |
| STM32H7xx | 2 | 2 MB | 256 KB | 16 |
| STM32G4xx | 1 | 512 KB | 96 KB | 16 |
| STM32L0xx | 1 | 128 KB | 20 KB | 8 |
| STM32L4xx | 1 | 1 MB | 128 KB | 16 |
| STM32WBxx | 1 | 1 MB | 256 KB | 16 |
| STM32G0xx | 1 | 128 KB | 20 KB | 8 |

---

## ۳. مکانیسم‌های ایمنی

ZenOS مکانیسم‌های ایمنی زیر را ارائه می‌دهد. بیشتر آنها به‌صورت **پیش‌فرض غیرفعال** هستند تا حجم حداقل باشد. آنها را با پرچم‌های `-D` در `ZenOS_Config.hpp` فعال کنید.

### ۳.۱ تشخیص سرریز پشته (`OS_SAFETY_SOFT_WATCHDOG`)

**مکانیسم:** بررسی canary + محدوده اشاره‌گر پشته. هر پشته تسک با الگوی canary شناخته‌شده (`0xDEADBEEF`) در 8 کلمه پایینی مقداردهی اولیه می‌شود.

**واکنش:** در صورت تشخیص سرریز، تسک مربوطه از صف اولویت حذف و ریست می‌شود (حداکثر `OS_SAFETY_TASK_MAX_RECOVERY` بار، پیش‌فرض 3).

**فرض:** تشخیص سرریز پشته *واکنشی* است — به‌صورت دوره‌ای در تایمر اجرا می‌شود.

### ۳.۲ بررسی یکپارچگی TCB (`OS_MONITOR_TCB_INTEGRITY`)

**مکانیسم:** عدد جادویی (`0x54434200`) در هر TCB نوشته و در بررسی‌های پشته اعتبارسنجی می‌شود.

### ۳.۳ پایش ددلاین (`OS_MONITOR_DEADLINE`)

**مکانیسم:** هر تسک می‌تواند ددلاین داشته باشد. اگر تسک در پنجره ددلاین اجرا نشود، خطا ثبت می‌شود.

**اقدامات پیکربندی** (`OS_SAFETY_DEADLINE_ACTION`):
- `0` — فقط ثبت (بدون اقدام)
- `1` — ریست تسک (پیش‌فرض)
- `2` — غیرفعال‌سازی دائمی تسک

### ۳.۴ ثبت خطا (`OS_MONITOR_ERROR_LOG`)

**مکانیسم:** حلقه بافر با اندازه ثابت (`OS_MONITOR_ERROR_LOG_SIZE`، پیش‌فرض 32) خطاها را با زمان، کد خطا، شناسه تسک و سطح شدت ثبت می‌کند.

### ۳.۵ تایمر سخت‌افزاری (`OS_SAFETY_HW_WATCHDOG`)

**مکانیسم:** ادغام با IWDG مستقل STM32. دو تابع تغذیه:
- `os_hw_watchdog_feed()` — تغذیه بدون قید و شرط
- `os_hw_watchdog_check()` — تغذیه مشروط: فقط اگر `error_count < 10` باشد

### ۳.۶ تایمر نرم‌افزاری (`OS_SAFETY_SOFT_WATCHDOG`)

**مکانیسم:** تشخیص تسک‌های گیر کرده با پایش `last_yield_tick`. اگر تسک در مدت `OS_SAFETY_SOFT_WDG_TIMEOUT_MS` (پیش‌فرض 3000ms) واگذاری نکند، ریست می‌شود.

### ۳.۷ آزمون RAM (`OS_SAFETY_RAM_TEST`)

**مکانیسم:** الگوریتم March C- پس‌زمینه برای یکپارچگی SRAM. هر فراخوانی `os_ram_test_step()` یک کلمه 32 بیتی را آزمایش می‌کند.

### ۳.۸ بررسی CRC (`OS_SAFETY_CRC_CHECK`)

**مکانیسم:** در زمان بوت، `os_crc_init()` CRC کل ناحیه فلش را محاسبه می‌کند. `os_crc_check_step()` فلش را در قطعات 64 کلمه‌ای دوباره بررسی می‌کند.

### ۳.۹ محافظت MPU (`OS_SAFETY_MPU`)

**مکانیسم:** واحد حفاظت حافظه سخت‌افزاری (ARMv7-M) با نواحی:
- 0: فلش (کد) — فقط خواندنی، قابل اجرا
- 1: SRAM (داده) — خواندنی/نوشتنی، غیرقابل اجرا
- 2: مدها — خواندنی/نوشتنی، غیرقابل اجرا
- 3: پشته هر تسک — خواندنی/نوشتنی، غیرقابل اجرا
- 4+: نواحی اضافی هر تسک

### ۳.۱۰ تایم‌اوت بخش حیاتی (`OS_SAFETY_MAX_CRITICAL_US`)

**مکانیسم:** بلاک‌های `OS_SAFE` با شمارنده چرخه DWT زمان‌بندی می‌شوند. اگر از حد فراتر رود، خطا ثبت می‌شود.

### ۳.۱۱ مدیر خطا (`OS_Fault_Handler`)

**مکانیسم:** تمام استثنائات خطا (HardFault, MemManage, BusFault, UsageFault) توسط `OS_Fault_Handler` گرفته می‌شوند و تسک خطا غیرفعال و تسک idle بازیابی می‌شود.

---

## ۴. مفروضات استفاده

### ۴.۱ ایجاد تسک

- **تمام تسک‌ها باید قبل از `os_start()` ایجاد شوند.** ایجاد تسک بعد از شروع زمان‌بند مجاز نیست.
- تسک‌ها به‌صورت ایستا تخصیص می‌یابند (قالب‌محور).
- اولویت‌ها باید برای تسک‌هایی که نیاز به ترتیب پیشی دارند یکتا باشند.

### ۴.۲ اندازه‌گیری پشته

- اندازه پیش‌فرض: `OS_KERNEL_STACK_SIZE = 512` بایت.
- از `os_get_stack_usage()` و `os_get_stack_report()` برای اندازه‌گیری مصرف واقعی استفاده کنید.
- پشته باید ≥130% اوج مصرف اندازه‌گیری‌شده باشد.

### ۴.۳ مدیریت وقفه

- ISRs باید از نسخه‌های امن (`signal_from_isr()`، `put_from_isr()`) استفاده کنند.
- `OS_SAFE` باید برای محافظت متغیرهای مشترک بین تسک‌ها و ISRs استفاده شود.

### ۴.۴ IPC

- **eventها:** شمارنده سیگنال انباشته. eventها با ID ≥ 32 باید به‌صورت جداگانه سیگنال داده شوند.
- **قفل‌ها:** قفل بازگشتی پشتیبانی می‌شود اما تعداد باز کردن باید با تعداد قفل برابر باشد.
- **سقف IPC:** سقف را روی اولویت بالاترین تسکی که قفل را به دست می‌آورد تنظیم کنید.

---

## ۵. محدودیت‌ها

### ۵.۱ محدودیت‌های معماری

| محدودیت | تأثیر | راه حل |
|---------|--------|--------|
| بدون ایجاد پویای تسک بعد از `os_start()` | عدم امکان ایجاد تسک در زمان اجرا | تمام تسک‌ها را از قبل ایجاد و شروع/توقف کنید |
| بدون زمان‌بند دوره‌ای | تسک‌های با اولویت مساوی باید دستی واگذاری کنند | از `os_yield()` در تسک‌های تعاونی استفاده کنید |
| فقط تک‌هسته‌ای (به جز STM32H7) | عدم موازی واقعی در MCU تک‌هسته‌ای | از ISRs برای کار همزمان با اولویت بالا استفاده کنید |
| بدون تخصیص حافظه پویا | عدم تغییر اندازه صفوف، پشته‌ها یا لیست‌های تسک | در زمان کامپایل بیش از حد تخصیص دهید |

### ۵.۲ مکانیسم‌های ایمنی

| مکانیسم | محدودیت | نتیجه |
|---------|---------|--------|
| canary پشته | واکنشی (در تایمر بررسی می‌شود) | سرریز بین بررسی‌ها شناسایی نمی‌شود |
| عدد جادو TCB | فقط 4 بایت خرابی خاص را تشخیص می‌دهد | خرابی که عدد جادو را حفظ می‌کند نامرئی است |
| پایش ددلاین | عدم اجرا را تشخیص می‌دهد، نه عدم تکمیل را | تسکی که اجرا می‌شود اما کار را تمام نمی‌کند شناسایی نمی‌شود |
| آزمون RAM | نوشتن غیرمخرب در حین ISR/DMA | مثبت کاذب ممکن است |
| تایمر سخت‌افزاری | ریست خارجی در تایم‌اوت | بدون خاموش‌سازی نرم؛ حافظه فرار از بین می‌رود |
| MPU | پنجره‌های کوتاه بدون محافظت در حین تغییر زمینه | شرایط رقابتی ممکن است |
| بررسی CRC | افزایشی (غیراتمی) | نوشتن فلش در حین بررسی مثبت کاذب ایجاد می‌کند |
| ثبت خطا | بافر حلقه‌ای، فقط RAM | در ریست سخت‌افزاری از بین می‌رود |

---

## ۶. مرجع پیکربندی

تمام ثابت‌های پیکربندی در `ZenOS_Config.hpp` هستند. آن‌ها را از طریق پرچم‌های `-D` کامپایلر یا در سرآغاز قبل از نگهبان‌های `#ifndef` بازنویسی کنید.

### ۶.۱ هسته

| ثابت | پیش‌فرض | محدوده | توضیح |
|------|---------|--------|--------|
| `OS_KERNEL_TICK_PERIOD_US` | 100 | 100–1000 | وضوح تیک به میکروثانیه |
| `OS_KERNEL_STACK_SIZE` | 512 | ≥64 | اندازه پشته پیش‌فرض به بایت |

### ۶.۲ ابزارها (IPC و زمان‌بندی)

| ثابت | پیش‌فرض | توضیح |
|------|---------|--------|
| `OS_TOOL_EVENT` | 1 | فعال‌سازی اصول اولیه event |
| `OS_TOOL_MUTEX` | 1 | فعال‌سازی قفل با ارث‌بری اولویت |
| `OS_TOOL_QUEUE` | 1 | فعال‌سازی صف FIFO محدود |
| `OS_TOOL_SEMAPHORE` | 1 | فعال‌سازی سیگنال شمارشی |
| `OS_TOOL_TICKLESS_IDLE` | 1 | فعال‌سازی بیکاری WFI برای صرفه‌جویی در توان |

### ۶.۳ پایش

| ثابت | پیش‌فرض | توضیح |
|------|---------|--------|
| `OS_MONITOR_DEADLINE` | 0 | فعال‌سازی پایش ددلاین |
| `OS_MONITOR_TCB_INTEGRITY` | 0 | فعال‌سازی بررسی عدد جادو TCB |
| `OS_MONITOR_ERROR_LOG` | 1 | فعال‌سازی بافر ثبت خطا |
| `OS_MONITOR_ERROR_LOG_SIZE` | 32 | ظرفیت ثبت خطا (ورودی) |

> **توجه:** `OS_MONITOR_DEADLINE` و `OS_MONITOR_TCB_INTEGRITY` به‌صورت پیش‌فرض غیرفعال هستند. برای IEC 62304 کلاس B/C و IEC 61508 SIL 2+ الزامی هستند.

### ۶.۴ ایمنی

| ثابت | پیش‌فرض | محدوده | توضیح |
|------|---------|--------|--------|
| `OS_SAFETY_RAM_TEST` | 0 | 0/1 | آزمون SRAM پس‌زمینه March C- |
| `OS_SAFETY_MPU` | 0 | 0/1 | محافظت حافظه سخت‌افزاری هر تسک |
| `OS_SAFETY_HW_WATCHDOG` | 0 | 0/1 | ادغام IWDG |
| `OS_SAFETY_CRC_CHECK` | 0 | 0/1 | بررسی یکپارچگی فلش |
| `OS_SAFETY_SOFT_WATCHDOG` | 0 | 0/1 | تشخیص تسک گیر کرده نرم‌افزاری |

> **توجه:** تمام ویژگی‌های ایمنی به‌صورت پیش‌فرض غیرفعال (`0`) هستند. آنها را صریحاً در `ZenOS_Config.hpp` با پرچم‌های `-D` فعال کنید، یا از ماکروهای اجرا (`-DOS_TARGET_MEDICAL`، `-DOS_TARGET_INDUSTRIAL`) استفاده کنید.
| `OS_SAFETY_DEADLINE_ACTION` | 1 | 0/1/2 | 0=ثبت، 1=ریست، 2=غیرفعال در تأخیر ددلاین |
| `OS_SAFETY_TASK_MAX_RECOVERY` | 3 | ≥0 | حداکثر تلاش بازیابی پشته قبل از غیرفعال‌سازی دائمی |
| `OS_SAFETY_SOFT_WDG_TIMEOUT_MS` | 3000 | >0 | تایم‌اوت تایمر نرم‌افزاری (ms) |
| `OS_SAFETY_MAX_CRITICAL_US` | 1000 | >0 | حداکثر مدت در OS_SAFE (µs) |

### ۶.۵ اجرای استاندارد ایمنی (IEC 62304 / IEC 61508)

ZenOS دو ماکرو اجرای زمان کامپایل ارائه می‌دهد. هنگام فعال‌سازی، ساخت با `#error` شکست می‌خورد اگر هر پیکربندی اجباری غیرفعال باشد.

```bash
# IEC 62304 — چرخه حیات نرم‌افزار پزشکی
-DOS_TARGET_MEDICAL=2        # کلاس A=1, B=2, C=3

# IEC 61508 — ایمنی عملکردی صنعتی
-DOS_TARGET_INDUSTRIAL=3     # سطح SIL 1=1, 2=2, 3=3, 4=4
```

**اجباری‌ها بر اساس استاندارد:**

| پیکربندی | پزشکی B | پزشکی C | صنعتی SIL 2 | صنعتی SIL 3 |
|----------|---------|---------|-------------|-------------|
| `OS_MONITOR_DEADLINE` | ✅ | ✅ | ✅ | ✅ |
| `OS_MONITOR_ERROR_LOG` | ✅ | ✅ | ✅ | ✅ |
| `OS_SAFETY_SOFT_WATCHDOG` | ✅ | ✅ | ✅ | ✅ |
| `OS_SAFETY_DEADLINE_ACTION` | ≥1 | ≥1 | ≥1 | ≥1 |
| `OS_SAFETY_HW_WATCHDOG` | — | ✅ | ✅ | ✅ |
| `OS_MONITOR_TCB_INTEGRITY` | — | ✅ | — | ✅ |
| `OS_SAFETY_MPU` | — | ✅ | — | ✅ |
| `OS_SAFETY_CRC_CHECK` | — | ✅ | — | ✅ |
| `OS_SAFETY_RAM_TEST` | — | ✅ | — | ✅ |

> **⚠️ توجه:** این اجرا فقط *تکمیل بودن پیکربندی* را تضمین می‌کند، نه انطباق را. یکپارچه‌ساز هنوز مسئول چرخه حیات کامل ایمنی (تحلیل خطر، V&V، مستندات) است.

---

## ۷. شیوه‌های یکپارچه‌سازی توصیه‌شده

1. **مصرف پشته را اندازه بگیرید** با `os_get_stack_usage()` و `os_get_stack_report()` در زمان توسعه.
2. **تایمر سخت‌افزاری را تغذیه کنید** از یک تسک اختصاصی با دوره ≤ 50% تایم‌اوت IWDG.
3. **از `os_error_expect_begin()`/`os_error_expect_end()`** در مسیرهای خطای ایمن شناخته‌شده استفاده کنید.
4. **ثبت خطا را دوره‌ای بخوانید** و در صورت نیاز به بقا در ریست، در حافظه غیرفرار ذخیره کنید.
5. **آزمون RAM را در idle اجرا کنید** با فراخوانی `os_ram_test_step()` از هوک idle.
6. **`os_crc_init()` را یکبار در بوت فراخوانی کنید** سپس `os_crc_check_step()` را دوره‌ای اجرا کنید.
7. **اولویت‌های سقف IPC را صحیح تنظیم کنید** — سقف هر قفل را روی اولویت بالاترین تسکی که آن را به دست می‌آورد تنظیم کنید.
8. **ویژگی‌های استفاده نشده را غیرفعال کنید** در `ZenOS_Config.hpp` برای کاهش حافظه.

---

## ۸. مرجع کدهای خطا

| کد | Enum | توضیح | علت معمول |
|----|------|--------|-----------|
| 1 | `SAFE_DELAY_MS` | تأخیر در OS_SAFE فراخوانی شد | باگ: فراخوانی بلاک در بخش حیاتی |
| 2 | `SAFE_YIELD` | واگذاری در OS_SAFE فراخوانی شد | باگ: واگذاری در بخش حیاتی |
| 3 | `SAFE_EVENT_WAIT` | انتظار event در OS_SAFE | باگ: IPC بلاک در بخش حیاتی |
| 4 | `STACK_OVERFLOW` | canary پشته یا بررسی SP شکست خورد | پشته تسک خیلی کوچک |
| 5 | `INVALID_EVENT_ID` | سیگنال event با شناسه خارج از محدوده | باگ: event نامعتبر |
| 6 | `TASK_AFTER_START` | ایجاد تسک بعد از os_start() | باگ: ایجاد بعد از شروع زمان‌بند |
| 7 | `TASK_STUCK` | تایم‌اوت تایمر نرم‌افزاری | تسک در مهلت واگذاری نمی‌کند |
| 8 | `SAFE_TOO_LONG` | OS_SAFE از حداکثر مدت فراتر رفت | باگ: بخش حیاتی طولانی |
| 9 | `HARDFAULT` | استثنای خطای CPU | دسترسی حافظه نامعتبر |
| 10 | `PRIORITY_CONFLICT` | تضاد سقف قفل شناسایی شد | پیکربندی نادرست اولویت سقف |
| 11 | `DEADLINE_MISS` | تسک ددلاین خود را از دست داد | تسک بیش از حد بارگذاری شده |
| 12 | `TCB_CORRUPTED` | عدد جادو TCB نامعتبر | خرابی حافظه |
| 13 | `SENSOR_TIMEOUT` | تایم‌اوت خواندن حسگر | مشکل مدها سخت‌افزاری |
| 14 | `SAFE_MUTEX_LOCK` | قفل mutex در OS_SAFE | باگ: قفل بلاک در بخش حیاتی |
| 15 | `RAM_TEST_FAIL` | آزمون RAM خطا شناسایی کرد | خرابی سخت‌افزاری SRAM |

---

## ۹. سلب مسئولیت

ZenOS تحت مجوز MIT به‌صورت AS-IS ارائه می‌شود. مکانیسم‌های ایمنی توصیف‌شده در این سند پیاده‌سازی‌های **بهترین تلاش** هستند که برای کمک به توسعه سیستم‌های تعبیه‌شده قوی در نظر گرفته شده‌اند. آن‌ها یک محصول ایمنی گواهی‌شده تشکیل نمی‌دهند. کاربر به‌تنهایی مسئول است:

- تأیید اینکه ZenOS نیازمندی‌های ایمنی برنامه خاص آن‌ها را برآورده می‌کند
- انجام تأیید رسمی، تست و گواهینامه طبق دامنه مربوطه (پزشکی، خودرویی، صنعتی و غیره)
- تضمین اینکه پیکربندی‌های سخت‌افزاری و نرم‌افزاری برای مورد استفاده هدف مناسب هستند

نویسندگان و مشارکت‌کنندگان هیچ مسئولیتی در قبال خسارات ناشی از استفاده از ZenOS در هیچ برنامه‌ای ندارند.
